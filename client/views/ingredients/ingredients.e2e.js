(function(){
  'use strict';

  describe('ingredients route', function () {

    beforeEach(function () {
      browser.get('/ingredients');
    });

    // it('should have a basic content', function () {
    //   expect(element.all(by.css('div')).first().getText()).toBe('Home Screen');
    // });

  });
}());
